import { ImgSecurePipe } from './img-secure.pipe';

describe('ImgSecurePipe', () => {
  it('create an instance', () => {
    const pipe = new ImgSecurePipe();
    expect(pipe).toBeTruthy();
  });
});
